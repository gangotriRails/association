<%= form_with(model: insta_user) do |form| %>
  <% if insta_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(insta_user.errors.count, "error") %> prohibited this insta_user from being saved:</h2>

      <ul>
        <% insta_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
